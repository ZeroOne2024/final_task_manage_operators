server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 2522
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true


app:
  admin:
    username : "admin"
    fullName : "Admin"
    phoneNumber: "+998944310717"
