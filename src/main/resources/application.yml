server:
  port: 80

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/support_bot_db
    username: postgres
    password: Sobirov0717
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate.format_sql: false

